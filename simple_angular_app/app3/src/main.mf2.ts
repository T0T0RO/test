// simple_angular_app/app3/src/main.mf2.ts
// MF2 build entry for Angular 18 remote (app3).
// This is ONLY for "ng build --configuration=mf2" so Angular compiles the app,
// but does NOT bootstrap to document. MF2 runtime uses src/bootstrap.ts instead.

import './app/app';
import './app/app.config';
import './app/app.routes';

export {};
